#compdef gr

typeset -A opt_args
local curcontext="$curcontext" context state line

_arguments \
  '1:git dir:_files -/' \
  ':git command:->command' \

case $state in
  (command)
    if ! typeset -f _git_commands >/dev/null 2>&1;then _git;fi
    _git_commands
    ;;
esac

return 0
