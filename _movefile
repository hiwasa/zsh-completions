#compdef movefile movefile.pl

typeset -A opt_args
local curcontext="$curcontext" context state line

_arguments \
  '1:files:_files' \
  '2:files:->reldir' \

case $state in
  (reldir)
    compadd $(_call_program rel find $(dirname $line[1]'/'$line[2]))
    ;;
esac

